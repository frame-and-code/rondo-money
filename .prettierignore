# Dependencies & generated output (also git-ignored)
node_modules/
**/dist/
**/build/
**/.next/
**/coverage/
**/generated/
.turbo/
.husky/_/

# Generated by Next.js (committed, but not ours to format)
**/next-env.d.ts

# The API contract (F1.4): written by `pnpm --filter @rondo/api openapi`, committed so a
# contract change is a reviewable diff. Formatting it here would guarantee a permanent
# difference between the committed file and the generator's output — which is exactly what
# the drift check compares.
apps/api/openapi.json

# Lockfile is managed by pnpm
pnpm-lock.yaml
