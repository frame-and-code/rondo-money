/**
 * @ffai/api — NestJS (REST) backend.
 *
 * The NestJS skeleton lands in F0.4, which brings its own framework tsconfig
 * (decorators, etc.). Placeholder for now so lint/typecheck have a real entry point.
 */
export {};
