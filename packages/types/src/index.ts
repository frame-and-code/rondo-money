/**
 * @ffai/types — shared DTOs and domain types for Fin Flow AI.
 *
 * Money is represented as integer minor units using BigInt. The number of
 * minor digits is derived from the budget currency (ISO 4217) and is never
 * hardcoded to 2. Concrete DTOs are added alongside the features that
 * introduce them (see the development plan / PRD).
 */
export {};
